import 'package:flutter/material.dart';

class FilterUiDesign extends StatelessWidget {
  const FilterUiDesign({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
